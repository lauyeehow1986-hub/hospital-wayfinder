# v0.3.1 — Floor map fixes

- Fix: NHCS cafe is on **Level 2**, not Level 1 — added an L2 cafe node + lift
  edge and re-anchored the POI; routing now crosses L1→L2 to reach it.
- Fix: floor-map handoff markers are now **departure-only**, so a level-change
  marker no longer renders on top of the destination pin and the arrow/label
  direction is correct (`mapView.handoffsForLevel`, unit-tested).
- Polish: re-spaced node coordinates, larger map canvas (340×300, min-height),
  edge-aware labels that don't overflow, label halos for legibility, and
  single-node "buildings" no longer draw clutter boxes.
- Service-worker cache bumped to `hw-v0.4`. 47 tests total.
